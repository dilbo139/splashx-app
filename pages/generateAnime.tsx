import UploadAnime from "@/components/UploadAnime";
import React from "react";

type Props = {};

const generateAnime = (props: Props) => {
  return <UploadAnime />;
};

export default generateAnime;
